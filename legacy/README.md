# Legacy Code

Este directorio contiene el código original del proyecto antes de la refactorización.

## Archivos

- `train_model.py` - Script básico de entrenamiento
- `lambda_function.py` - Handler Lambda original
- `deploy.sh` - Script de despliegue bash
- `requirements.txt` - Dependencias originales

## Nota

Este código se mantiene como referencia. El código de producción está en `src/ml_lambda/`.
